SELECT * FROM products
-- WHERE name LIKE 'b%'
--  search for the name starts with 'captial or small b'
-- WHERE name LIKE '%b%'
-- search for the name which has b anywhere on the list
WHERE name LIKE '___t'
-- search for the missing pattern on the list from name